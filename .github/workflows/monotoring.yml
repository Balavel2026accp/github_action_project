name: Monitor

on:
  workflow_run:
    workflows:
      - "Example 1 testing github action"
    types:
      - completed

jobs:
    job_1:
     runs-on: ubuntu-latest
     steps:
        - name: execute monitoring scripts
          run: ./system_monitoring.bash

